# DataPilot 使用指南 — 怎么说指令

> 核心原则：像跟人说话一样自然，不用记公式。说人话就行。

---

## 一、最常用的操作

### 去重
| 你想做什么 | 就说 |
|-----------|------|
| 去掉重复行 | `删除重复数据` |
| 按某列去重 | `根据姓名列删除重复` |
| 保留唯一值 | `只保留不重复的数据` |

### 筛选
| 你想做什么 | 就说 |
|-----------|------|
| 按条件筛选 | `筛选出部门是技术部的数据` |
| 多条件 | `筛选部门是技术部且工资大于10000的数据` |
| 包含 | `筛选出姓名包含"张"的` |
| 排除 | `去掉工资小于5000的` |

### 排序
| 你想做什么 | 就说 |
|-----------|------|
| 升序/降序 | `按基本工资降序排序` |
| 多列排序 | `先按部门升序，再按工资降序` |
| 取前N名 | `按工资从高到低取前10名` |

### 分组统计
| 你想做什么 | 就说 |
|-----------|------|
| 分组汇总 | `统计每个部门的平均工资` |
| 多维度 | `统计每个部门每个岗位的人数` |
| 总计 | `计算所有人的工资总和` |

### 公式计算
| 你想做什么 | 就说 |
|-----------|------|
| 加减乘除 | `计算实发工资=基本工资+奖金-扣款` |
| IF条件 | `如果工资大于10000则显示"高薪"，否则显示"普通"` |
| 文本处理 | `提取身份证号的前6位` |
| 日期处理 | `计算入职日期到今天的年数` |

### 列操作
| 你想做什么 | 就说 |
|-----------|------|
| 只看某些列 | `只看姓名和工资两列` |
| 删除列 | `删除备注列` |
| 重命名 | `把工资列改名为月收入` |

---

## 二、进阶操作

### 两张表匹配
| 就说 |
|-----|
| `把表2的部门负责人匹配到表1，根据部门名称匹配` |

### 垂直合并
| 就说 |
|-----|
| `把表1和表2合并在一起` |

### 数据清洗
| 就说 |
|-----|
| `删除空行` |

### 批量更新
| 就说 |
|-----|
| `把部门为技术部的工资改成15000` |

### 多步串联
| 就说 |
|-----|
| `筛选出技术部的数据，然后按工资降序排序` |
| `先统计每个部门的平均工资，然后只看平均工资大于8000的` |

---

## 三、实用场景举例

```
场景：员工表中有重复录入
→ 说「根据姓名列删除重复」

场景：想看看工资最高的几个人
→ 说「按工资降序排序，取前5名」

场景：统计各部门工资情况
→ 说「统计每个部门的平均工资、最高工资和最低工资」

场景：给技术部涨工资
→ 说「把部门为技术部的工资改成15000」

场景：两张表合并分析
→ 说「把表2的岗位级别匹配到表1，根据姓名匹配」

场景：做个工资调整
→ 说「如果工资小于5000，则新工资=工资*1.2，否则新工资=工资*1.1」

场景：复杂多步
→ 说「筛选出入职日期在2020年之后的数据，按部门分组统计平均工资，然后只看平均工资大于8000的部门」
```

---

## 四、小技巧

1. **说不明白就多说两句** — 支持链式表达，用`再`、`然后`连接多个操作
2. **列名说中文** — 直接说Excel表头的名字就行，"部门"、"姓名"、"工资"
3. **说错了没关系** — 有撤销功能，可以回退到上一步
4. **尽量具体** — `筛选工资大于10000的` 比 `筛选高工资的` 更准确
